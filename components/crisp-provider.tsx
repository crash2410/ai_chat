"use client"

import {CrispChat} from "@/components/crisp-chat";

/**
 * Компонент, предоставляющий провайдер Crisp.
 *
 * Этот компонент использует компонент CrispChat.
 *
 * @returns {JSX.Element} - Возвращает компонент CrispChat.
 */
export const CrispProvider = () => {
    return <CrispChat/>
}